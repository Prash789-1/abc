const getAllUsers = (id) => monads

const getuser = (id) => {
    try {
        let result = getuser.findAll(user);
        console.log(result)
        result.status(200).send(result);
    } catch (err) {
        console.error(err);
    }
}
this file is created by parshant

